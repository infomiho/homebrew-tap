# homebrew-tap

Homebrew casks for [infomiho](https://github.com/infomiho) apps.

```sh
brew install --cask infomiho/tap/diktafon
```

The release workflow of each app updates its cask when a version is tagged.
